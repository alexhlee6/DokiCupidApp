<% if current_user %>
<script>
  window.currentUser = {
    "id": <%= current_user.id %>,
    "username": "<%= current_user.username %>",
    "photoUrl": "<%= current_user.photo.attached? ? url_for(current_user.photo) : "" %>",
    "profileId": "<%= current_user.profile.id if current_user.profile %>"
  };
</script>
<% end %>

<div id="root">
</div>